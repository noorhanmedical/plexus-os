version: 1.0
runtime: nodejs18

build:
  commands:
    pre-build:
      - npm ci
    build:
      - npm run build

run:
  command: npm start
  network:
    port: 5000
    env: PORT
  env:
    - name: PORT
      value: "5000"
