version: 1.0
runtime: nodejs16

build:
  commands:
    build:
      - npm ci
      - npm run build

run:
  command: npm start
  network:
    port: 5000
  env:
    - name: NODE_ENV
      value: production
    - name: OPENAI_API_KEY
      value: PLACEHOLDER_WILL_CONFIGURE_IN_CONSOLE
