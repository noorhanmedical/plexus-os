version: 1.0
runtime: nodejs18

build:
  commands:
    pre-build:
      - npm ci --include=dev
    build:
      - npm run build

run:
  command: npm start
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
